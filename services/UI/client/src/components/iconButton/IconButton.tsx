import { ButtonProps } from "@/components/button/types/Button";
import { IconButtonStyled } from "@/components/iconButton/styles/IconButton.styled";

export const IconButton = (props: ButtonProps) => <IconButtonStyled.IconButton {...props} />;
